# Secrets — never commit
.env
.env.*
*.pem
/tmp/.sigma_token

# Upstream skill mirror (cloned on demand by scripts/refresh-vendor.sh)
vendor/

# OS / editor junk
.DS_Store
*.swp
.idea/
.vscode/

# Python
__pycache__/
*.pyc

# Local Netlify folder
.netlify

# Per-build QA render scratch (PNG screenshots), not permanent repo content
skills/*/shots/
