# Secrets — never commit
.env
.env.*
*.pem
/tmp/.sigma_token

# Upstream skill mirror (cloned on demand by scripts/refresh-vendor.sh)
vendor/

# OS / editor junk
.DS_Store
*.swp
.idea/
.vscode/

# Python
__pycache__/
*.pyc

# Local Netlify folder
.netlify

# Per-build QA renders and local push-state tracking (sigma-company-dashboard-v2) —
# machine-local by design, see HANDOFF.md's wb_state_<key>.json note.
skills/sigma-company-dashboard-v2/shots/
skills/sigma-company-dashboard-v2/specs/
